function createChessBoard(spaceForBoardId){

    const spaceForBoard = document.getElementById(spaceForBoardId);

    const chessBoard = document.createElement("table");
    const row1 = document.createElement("tr");
}